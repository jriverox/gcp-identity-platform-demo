# Puerto en que escucha la API (por defecto 8080)
PORT=8080

# API Key de Identity Toolkit (CIP)
API_KEY=AIzaSy…TuClaveDeAPI…

# Origen permitido para CORS (tu frontend local)
CORS_ORIGIN=http://localhost:8000

# (Opcional) Si prefieres que Admin SDK cargue las credenciales
# vía variable de entorno en vez de require('../key.json'):
GOOGLE_APPLICATION_CREDENTIALS=./key.json
